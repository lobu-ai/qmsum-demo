[project]
name = "qmsum-benchmark"
version = "0.1.0"
description = "ROUGE-L benchmark runner for the QMSum Lobu agent."
requires-python = ">=3.11"
dependencies = [
    "rouge-score>=0.1.2",
    "requests>=2.31",
]

[tool.uv]
package = false
