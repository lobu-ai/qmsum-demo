node_modules/
dist/
*.log

# Lockfile is gitignored until @lobu/promptfoo-provider lands on npm — local
# install paths leak into bun.lock and aren't portable. Once published, drop
# this entry and commit the lockfile.
bun.lock
bun.lockb

# Local environment
.env
.env.local

# Lobu local-run data dir (PGlite, encryption key cache, etc.)
.lobu-data/

# QMSum dataset — clone separately, never committed
data/

# Benchmark output
benchmark-results.json
benchmark-results/

# uv / Python virtualenv
.venv/
__pycache__/
*.pyc

# pi (PR-review CLI) runtime artifacts
.pi/

# Editor / OS noise
.DS_Store
.vscode/
.idea/
